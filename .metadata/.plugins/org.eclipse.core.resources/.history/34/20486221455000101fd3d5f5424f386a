package com.Assignments;

import java.time.Duration;
import java.util.List;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.interactions.Actions;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;

/*21.Open https://istqb.in/
		a.Display all 8 menus
*/
public class A21_DisplayAllMenus {

	public static void main(String[] args) throws InterruptedException {
		WebDriver driver = new ChromeDriver();
		driver.manage().window().maximize();
		driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(10));
		driver.get("https://istqb.in/");
		Actions act = new Actions(driver);
		
		WebElement abtMenu = driver.findElement(By.linkText("ABOUT US"));
		act.moveToElement(abtMenu).perform();
		//ABOUT US>SubMenus
		Thread.sleep(5000);
		List<WebElement>abtMenus = driver.findElements(By.xpath("//*[@id=\"sp-menu\"]/div/nav/ul/li[2]/div/div/div/div/ul[1]/li"));
		for(WebElement ab : abtMenus)
			System.out.println(ab.getText());
		//FOUNDATION>SubMenus
		act.moveToElement(driver.findElement(By.linkText("FOUNDATION"))).perform();
		WebDriverWait wait = new WebDriverWait(driver,Duration.ofSeconds(10));
		driver.findElement(By.linkText("FOUNDATION"));
		
		Thread.sleep(5000);
		WebElement foundationMenu = driver.findElement(By.linkText("ABOUT US"));
		act.moveToElement(foundationMenu).perform();
		
		
		
		driver.close();
		
		
	}

}
